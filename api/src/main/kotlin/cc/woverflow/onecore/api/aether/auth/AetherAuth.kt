package cc.woverflow.onecore.api.aether.auth

import java.nio.ByteBuffer

/*
Implements AetherToken auth.
Can be used directly with normal protocol or with AetherProtocol.
Not ready yet. May be deprecated or removed at any time.
 */
object AetherAuth {
    fun parse(aerData: String) {
        // do the parse thing
    }
    fun parseBytes(aerData: ByteBuffer) {
        // do the parse bytes thing
    }
}